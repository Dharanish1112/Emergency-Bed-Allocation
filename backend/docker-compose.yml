version: '3.8'

services:
  # Backend API
  backend:
    build: .
    container_name: emergency-backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      MONGODB_URI: mongodb+srv://dharanish1112_db_user:snWn9S6kpNU6UeBA@hca.8fkc3uf.mongodb.net/emergency-bed-booking?retryWrites=true&w=majority
      JWT_SECRET: your-super-secret-jwt-key-change-this-in-production
      PORT: 5001
      FRONTEND_URL: https://emergency-bed-allocation.onrender.com
    ports:
      - "5001:5001"
    networks:
      - emergency-network
    volumes:
      - ./logs:/app/logs

networks:
  emergency-network:
    driver: bridge
